packages:
    - name: scrape
      functions:
        - name: get_menu
          runtime: python:default
          web: true
          environment:
            DATABASE_URL: "${DATABASE_URL}"
          limits:
            timeout: 30000
            memory: 512


